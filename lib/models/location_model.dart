// Modèle pour représenter une position géographique
class PositionModel {
  double latitude; // Latitude de la position
  double longitude; // Longitude de la position

  // Constructeur pour initialiser les coordonnées
  PositionModel({required this.latitude, required this.longitude});
}
